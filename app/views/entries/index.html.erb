<h2>Entries for <%=@project.name%></h1>
<% if !@entries.empty? %>
  <ul>
    <% @entries.each do |e| %>
      <li><%= "Hours: #{e.hours} Minutes: #{e.minutes}"%></li>
    <%end%>
  </ul>
  <h3>Total month: <%=@total_month_hours%></h3>
<%end%>
