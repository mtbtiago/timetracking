<h2>New entry for <%=link_to(@project.full_name,project_path(@project))%></h1>

<% if @entry.errors.any? %>
  <ul>
    <% @entry.errors.full_messages.each do |error_msg| %>
    <li><%= error_msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>