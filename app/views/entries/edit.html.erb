<% if @entry.errors.any? %>
  <ul>
    <% @entry.errors.full_messages.each do |error_msg| %>
    <li><%= error_msg %></li>
    <% end %>
  </ul>
<% end %>

<h2>Edit entry for <%=link_to(@project.full_name,project_path(@project))%></h2>

<%= render 'form' %>