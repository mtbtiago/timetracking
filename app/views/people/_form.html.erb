<%= form_for @person, :html => { :multipart => true } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, size: 24 %>

  <%= f.submit 'Save' %>
  <%= link_to("Cancel",people_path) %>

<% end %>
