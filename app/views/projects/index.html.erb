<h1>Last 10 projects</h1>
<ul>
<% @projects.each do |p| %>
<!-- <li><%= "Name: #{p.name}"%><%= (p.description) ? ". Desc: #{p.description}" : "" %></li> -->
<!-- <li><%= "Name: #{p.name}"%><%= (p.description.present?) ? ". Desc: #{p.description}" : "" %></li> -->
<!--<li><a href="<%=link_to(p.name,project_entries_path(p))%>"><%=p.full_name%></a></li> -->
<!-- You don't need to use <a> -->
  <li><%=link_to(p.full_name,project_entries_path(p))%></li>
<%end%>
</ul>
